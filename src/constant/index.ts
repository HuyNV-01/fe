export const EMPTY_STRING = "";

export const VALIDATION_CONFIG = {
  MIN_LENGTH_STRING: 3,
  MEDIUM_LENGTH_STRING: 100,
  MAX_LENGTH_STRING: 255,
  LENGTH_STRING: 8,
  MIN_LENGTH_NUMBER: 1,
  MIN_LENGTH_LOW_CASE: 1,
  MIN_LENGTH_UP_CASE: 1,
  MIN_SYMBOL: 1,
};

export const DEFAULT_LENGTH_OTP = 6;

export const DEFAULT_TIMEOUT_OTP = 60 * 3; // count down 180s

export const KEY_TIMEOUT_OTP = "otp_forgot_password_timer";

export const DEFAULT_LIMIT_INBOX = 20;

export const DEFAULT_LIMIT_MESSAGE = 20;

export const DEFAULT_LIMIT_SEARCH = 6;

export const DEFAULT_LIMIT_CONTACTS = 20;

export const DEFAULT_PAGE = 1;

export const UNKNOWN = "Unknown";

export const NOTIFICATION_MESSAGES = 99;

export const MAP_ARRAY = [1, 2, 3, 4, 5];

export const STATIC_DETAIL_ROUTES = ["add-new", "directory"];
